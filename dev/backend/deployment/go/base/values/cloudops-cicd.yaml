base:
  envName: dev
  namespace: cloudops-dev

app:
  serviceName: cloudops-cicd
  servicePort: 80
  ingressPath: /api/v1/cicd
  imageTag: main-1
  replicas: 2

service:
  targetPort: 8080
  type: ClusterIP
